// app configuration
// centralizes api endpoints and other config values
// uploads folder is used to store book cover images
const config = {
    API_URL: 'http://localhost:5001',
    UPLOADS_URL: 'http://localhost:5001/uploads'
};

export default config;